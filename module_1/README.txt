To run this program:
1) Open a powershell or command instance and cd to the project folder
2) Run the command "python run.py" (without the quotation marks)
3) Open a browser and navigate to http://127.0.0.1:8080 (or your corresponding localhost)